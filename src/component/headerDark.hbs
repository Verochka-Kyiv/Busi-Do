{{#with data}}
<div class="upHeader">
    <a href="#test">
        <div class="logo">
            <div class="badge">
                {{brandName}}
            </div>
        </div>
    </a>
    {{#with links}}
    <nav class="header_nav">
        <div class="nav nav_inline">
            {{#each this}}
            <a href="{{url}}" class="nav_link">{{text}}</a>
            {{/each}}
        </div>

    </nav>
    {{/with}}
</div>
{{/with}}